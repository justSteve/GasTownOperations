{
  "name": "@ecc/orchestrator",
  "version": "0.1.0",
  "description": "Agent Orchestration Engine for Claude Code entities",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "build:types": "tsc --emitDeclarationOnly",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "claude-code",
    "agent",
    "orchestration",
    "ecc"
  ],
  "author": "gtOps",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@ecc/materializer": "workspace:*"
  },
  "peerDependenciesMeta": {
    "@ecc/materializer": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
